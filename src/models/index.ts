import { User } from './user.model';
import { Post } from './post.model';
import { Comment } from './comments.model';
import { RefreshToken } from './refresh-token.model';

export const models = [User, Post, Comment, RefreshToken];
